import React from "react";
import Layout from "../Layout";

const UpdatePage = (props) => {
	return (
		<Layout title="Update Some Data">
			Ini page untuk mengubah data di database
		</Layout>
	);
};

export default UpdatePage;
