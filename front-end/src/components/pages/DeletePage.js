import React from "react";
import Layout from "../Layout";

const DeletePage = (props) => {
	return (
		<Layout title="Delete Existing Data">
			Ini page untuk mengapus data
		</Layout>
	);
};

export default DeletePage;
